---
phase: 07-dashboard-new-inspection-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/inspection/presentation/new_inspection_page.dart
  - lib/common/widgets/form_type_card.dart
  - lib/common/widgets/widgets.dart
autonomous: false
agents: ["engineering-mobile-app-builder"]
requirements: ["SCREEN-03"]
user_setup: []

must_haves:
  truths:
    - "New inspection form is organized into 3 ExpansionTile sections: Client Info, Property Info, Form Selection"
    - "FormTypeCard widget replaces plain CheckboxListTile for form type selection"
    - "ReachZoneScaffold provides sticky-bottom Continue action"
    - "All form validation logic preserved (email, date, year built, at-least-one-form)"
    - "Zero hardcoded Colors, EdgeInsets, or TextStyle values in new_inspection_page.dart"
  artifacts:
    - path: "lib/features/inspection/presentation/new_inspection_page.dart"
      provides: "Redesigned new inspection page with section grouping and progressive disclosure"
      min_lines: 250
      contains: "ExpansionTile"
    - path: "lib/common/widgets/form_type_card.dart"
      provides: "Visual form type selection card with description"
      min_lines: 30
      contains: "FormTypeCard"
  key_links:
    - from: "lib/features/inspection/presentation/new_inspection_page.dart"
      to: "lib/common/widgets/form_type_card.dart"
      via: "Form type selection widget"
      pattern: "FormTypeCard"
    - from: "lib/common/widgets/widgets.dart"
      to: "lib/common/widgets/form_type_card.dart"
      via: "Barrel export"
      pattern: "form_type_card.dart"
---

<objective>
Redesign the new inspection page with section grouping, progressive disclosure via ExpansionTile, and visual form type cards replacing plain checkboxes.

Purpose: Transform the wall-of-fields form into an organized, progressively-disclosed form that reduces cognitive load for field inspectors working in challenging conditions.
Output: lib/features/inspection/presentation/new_inspection_page.dart, lib/common/widgets/form_type_card.dart
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-dashboard-new-inspection-redesign/07-CONTEXT.md
@.planning/specs/07-dashboard-new-inspection-redesign-spec.md

Relevant source files:
@lib/features/inspection/presentation/new_inspection_page.dart
@lib/features/inspection/domain/form_type.dart
@lib/features/inspection/domain/inspection_setup.dart
@lib/features/inspection/domain/inspection_draft.dart
@lib/theme/tokens.dart
@lib/theme/palette.dart
@lib/theme/typography.dart
@lib/theme/extensions.dart
@lib/theme/context_ext.dart
@lib/common/widgets/widgets.dart
@lib/common/widgets/reach_zone_scaffold.dart
@lib/common/widgets/app_text_field.dart
@lib/common/widgets/section_card.dart
@lib/common/widgets/section_header.dart
@lib/common/widgets/app_button.dart
@lib/common/widgets/app_checkbox_tile.dart
@lib/app/routes.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure form into 3 ExpansionTile sections</name>
  <files>lib/features/inspection/presentation/new_inspection_page.dart</files>
  <action>
Rewrite the new_inspection_page.dart widget tree to organize the form into 3 collapsible sections using ExpansionTile, wrapped in ReachZoneScaffold.

**Layout structure:**
1. `ReachZoneScaffold`:
   - `stickyBottom`: `AppButton` variant="filled" with `isThumbZone: true`, label "Continue", onPressed triggers form validation + save (preserve existing save logic)
   - `body`: `Form` wrapping a `ListView` with 3 `ExpansionTile` children

2. **Section 1 — Client Information** (ExpansionTile):
   - Title: "Client Information" styled with `context.appTokens.sectionHeader`
   - Initially expanded: `true`
   - Children: 3 `AppTextField` fields with `AppSpacing.spacingMd` gaps between them:
     - Client Name (required, textInputAction: TextInputAction.next)
     - Client Email (required, email keyboard, email validation)
     - Client Phone (required, phone keyboard)
   - Preserve all existing TextEditingControllers and validators

3. **Section 2 — Property Information** (ExpansionTile):
   - Title: "Property Information"
   - Initially expanded: `true`
   - Children: 3 `AppTextField` fields:
     - Property Address (required)
     - Inspection Date (required, date format YYYY-MM-DD, datetime keyboard)
     - Year Built (required, number keyboard, range 1800 to current+1)
   - Preserve all existing validators

4. **Section 3 — Form Selection** (ExpansionTile):
   - Title: "Inspection Forms"
   - Initially expanded: `true`
   - Children: FormTypeCard widgets (see Task 2)

**ExpansionTile theming:**
Style ExpansionTiles with design tokens:
- `backgroundColor`: `Theme.of(context).colorScheme.surface`
- `collapsedBackgroundColor`: `Theme.of(context).colorScheme.surface`
- `iconColor`: `Theme.of(context).colorScheme.primary`
- `tilePadding`: `AppEdgeInsets.pageHorizontal`
- `childrenPadding`: `EdgeInsets.symmetric(horizontal: AppSpacing.spacingLg, vertical: AppSpacing.spacingSm)`
- Shape: `RoundedRectangleBorder(borderRadius: BorderRadius.circular(AppRadii.radiusMd))`

**All styling must use design tokens** — zero hardcoded Colors, EdgeInsets, TextStyle literals.

> verification: grep -q "ExpansionTile" lib/features/inspection/presentation/new_inspection_page.dart
> verification: grep -q "ReachZoneScaffold" lib/features/inspection/presentation/new_inspection_page.dart
> verification: grep -c "Client Information" lib/features/inspection/presentation/new_inspection_page.dart | xargs test 1 -eq
> verification: grep -c "Property Information" lib/features/inspection/presentation/new_inspection_page.dart | xargs test 1 -eq
> verification: grep -c "Inspection Forms\|Form Selection" lib/features/inspection/presentation/new_inspection_page.dart | xargs test 1 -le
  </action>
  <verify>
grep -q "ExpansionTile" lib/features/inspection/presentation/new_inspection_page.dart
grep -q "ReachZoneScaffold" lib/features/inspection/presentation/new_inspection_page.dart
grep -c "Client Information" lib/features/inspection/presentation/new_inspection_page.dart | xargs test 1 -eq
grep -c "Property Information" lib/features/inspection/presentation/new_inspection_page.dart | xargs test 1 -eq
grep -c "Inspection Forms\|Form Selection" lib/features/inspection/presentation/new_inspection_page.dart | xargs test 1 -le
  </verify>
  <done>New inspection form organized into 3 themed ExpansionTile sections with ReachZoneScaffold layout.</done>
</task>

<task type="auto">
  <name>Task 2: Create FormTypeCard widget replacing CheckboxListTile</name>
  <files>lib/common/widgets/form_type_card.dart, lib/common/widgets/widgets.dart, lib/features/inspection/presentation/new_inspection_page.dart</files>
  <action>
Create a new `FormTypeCard` widget that provides a visual card for form type selection, replacing the plain `CheckboxListTile`.

**Create `lib/common/widgets/form_type_card.dart`:**

```dart
/// Visual card for selecting an inspection form type.
/// Displays the form label, a brief description, and a selection toggle.
class FormTypeCard extends StatelessWidget {
  const FormTypeCard({
    super.key,
    required this.label,
    required this.description,
    required this.selected,
    required this.onChanged,
  });

  final String label;
  final String description;
  final bool selected;
  final ValueChanged<bool> onChanged;
}
```

**Card design:**
- Use `SectionCard` or a custom `Card` with design tokens
- Selected state: card border uses `colorScheme.primary` with 2dp width, slight surface tint
- Unselected state: card border uses `colorScheme.surfaceVariant`
- Content layout:
  - Leading: Checkbox or custom toggle (themed with primary color)
  - Title: form label in `titleMedium` weight 600
  - Subtitle: description in `bodySmall` with `onSurfaceVariant` color
- Tap target: entire card is tappable (toggles selection), min 48dp height
- Spacing: `AppSpacing.spacingSm` gap between cards

**Form type descriptions** (hardcoded in new_inspection_page since they're presentation-level):
```dart
const _formDescriptions = {
  FormType.fourPoint: 'Electrical, HVAC, plumbing, and water heater inspection',
  FormType.roofCondition: 'Roof age, condition, and remaining useful life assessment',
  FormType.windMitigation: 'Wind resistance features and discount qualification',
};
```

**Add barrel export:**
Add `export 'form_type_card.dart';` to `lib/common/widgets/widgets.dart`.

**Integrate in new_inspection_page.dart:**
Replace the `CheckboxListTile` loop in the Form Selection section with `FormTypeCard` widgets:
```dart
...FormType.values.map((form) => Padding(
  padding: EdgeInsets.only(bottom: AppSpacing.spacingSm),
  child: FormTypeCard(
    label: form.label,
    description: _formDescriptions[form] ?? '',
    selected: _selectedForms.contains(form),
    onChanged: (selected) => setState(() {
      selected ? _selectedForms.add(form) : _selectedForms.remove(form);
    }),
  ),
)),
```

Preserve the "at least one form" validation in the save logic.

> verification: test -f lib/common/widgets/form_type_card.dart
> verification: grep -q "FormTypeCard" lib/common/widgets/form_type_card.dart
> verification: grep -q "form_type_card.dart" lib/common/widgets/widgets.dart
> verification: grep -q "FormTypeCard" lib/features/inspection/presentation/new_inspection_page.dart
> verification: grep -c "CheckboxListTile" lib/features/inspection/presentation/new_inspection_page.dart | xargs test 0 -eq
  </action>
  <verify>
test -f lib/common/widgets/form_type_card.dart
grep -q "FormTypeCard" lib/common/widgets/form_type_card.dart
grep -q "form_type_card.dart" lib/common/widgets/widgets.dart
grep -q "FormTypeCard" lib/features/inspection/presentation/new_inspection_page.dart
grep -c "CheckboxListTile" lib/features/inspection/presentation/new_inspection_page.dart | xargs test 0 -eq
  </verify>
  <done>FormTypeCard widget created with visual card design, integrated into form selection section, CheckboxListTile fully replaced.</done>
</task>

<task type="auto">
  <name>Task 3: Apply design tokens and finalize</name>
  <files>lib/features/inspection/presentation/new_inspection_page.dart</files>
  <action>
Finalize the new inspection page redesign by ensuring complete design token compliance and verifying all functionality.

**Design token sweep:**
1. Search for any remaining hardcoded values:
   - `Colors.` → Replace with `Theme.of(context).colorScheme.*` or `context.appTokens.*`
   - `EdgeInsets(` or `EdgeInsets.` (non-AppEdgeInsets) → Replace with `AppEdgeInsets.*` or `EdgeInsets.*` using `AppSpacing.*` constants
   - `TextStyle(` → Replace with `Theme.of(context).textTheme.*` or `context.appTokens.*`
   - `SizedBox(height:` with literal numbers → Replace with `SizedBox(height: AppSpacing.*)`
   - `BorderRadius.circular(` with literal numbers → Replace with `BorderRadius.circular(AppRadii.*)`

2. **AppBar styling**: Title "New Inspection" styled via theme (automatic from AppTheme.dark())

3. **Form field styling**: All `AppTextField` instances should use the component's built-in theming. Add `textInputAction: TextInputAction.next` to all fields except the last in each section (use `TextInputAction.done` for the last).

4. **Error handling**: Preserve SnackBar error display for save failures. Use `SnackbarHelper` if available, or style the SnackBar with design tokens.

5. **Loading state**: Preserve the loading/saving state that disables the Continue button. Use `LoadingOverlay` or the existing pattern.

**Final verification:**
1. All 6 TextEditingControllers preserved and properly disposed
2. All validators preserved (email regex, date range, year range, at-least-one-form)
3. Save logic preserved (InspectionSetup creation with UUID, repository.create, navigation to checklist)
4. Form key and validation flow preserved
5. Run: `dart analyze lib/features/inspection/presentation/new_inspection_page.dart` — zero issues
6. Run: `dart analyze lib/common/widgets/form_type_card.dart` — zero issues

> verification: grep -c "Colors\." lib/features/inspection/presentation/new_inspection_page.dart | xargs test 0 -eq
> verification: grep -c "Colors\." lib/common/widgets/form_type_card.dart | xargs test 0 -eq
> verification: dart analyze lib/features/inspection/presentation/new_inspection_page.dart 2>&1 | grep -q "No issues found"
> verification: dart analyze lib/common/widgets/form_type_card.dart 2>&1 | grep -q "No issues found"
  </action>
  <verify>
grep -c "Colors\." lib/features/inspection/presentation/new_inspection_page.dart | xargs test 0 -eq
grep -c "Colors\." lib/common/widgets/form_type_card.dart | xargs test 0 -eq
dart analyze lib/features/inspection/presentation/new_inspection_page.dart 2>&1 | grep -q "No issues found"
dart analyze lib/common/widgets/form_type_card.dart 2>&1 | grep -q "No issues found"
  </verify>
  <done>New inspection page fully redesigned with design tokens, all validation preserved, dart analyze passes with zero issues.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Form organized into 3 ExpansionTile sections (Client Info, Property Info, Form Selection)
- [ ] All sections start expanded
- [ ] FormTypeCard widgets replace CheckboxListTile with visual descriptions
- [ ] ReachZoneScaffold provides sticky-bottom "Continue" action
- [ ] All 6 form validators preserved and functional
- [ ] Save logic creates InspectionSetup and navigates to checklist
- [ ] TextEditingControllers properly disposed
- [ ] Zero hardcoded Colors, EdgeInsets, TextStyle values
- [ ] FormTypeCard exported from widgets barrel
- [ ] `dart analyze` passes with no issues for both files
</verification>

<success_criteria>
- New inspection form renders with 3 collapsible sections reducing visual clutter
- Form types displayed as visual cards with descriptions (not plain checkboxes)
- Progressive disclosure allows sections to expand/collapse
- All existing form validation and submission logic works identically
- Design tokens used exclusively (zero hardcoded values)
- FormTypeCard is a reusable component in the shared widget library
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-new-inspection-redesign/07-02-SUMMARY.md`

Update STATE.md: Plan 07-02 complete
</output>
