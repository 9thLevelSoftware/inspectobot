---
phase: 07-dashboard-new-inspection-redesign
plan: 04
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - test/features/inspection/new_inspection_page_test.dart
autonomous: false
agents: ["testing-evidence-collector"]
requirements: ["SCREEN-03"]
user_setup: []

must_haves:
  truths:
    - "All 4 existing new inspection tests updated to work with ExpansionTile-based layout"
    - "Progressive disclosure test verifies sections expand and collapse"
    - "FormTypeCard test verifies visual card selection toggles correctly"
    - "Form description visibility test verifies form type descriptions are shown"
    - "All tests pass: flutter test test/features/inspection/new_inspection_page_test.dart"
  artifacts:
    - path: "test/features/inspection/new_inspection_page_test.dart"
      provides: "Complete widget test coverage for redesigned new inspection page"
      min_lines: 180
      contains: "ExpansionTile"
  key_links:
    - from: "test/features/inspection/new_inspection_page_test.dart"
      to: "lib/features/inspection/presentation/new_inspection_page.dart"
      via: "Widget under test"
      pattern: "NewInspectionPage"
---

<objective>
Update and expand new inspection page widget tests to cover the section-based layout, progressive disclosure, and visual form type cards.

Purpose: Ensure the redesigned new inspection form maintains all existing validation and submission contracts while gaining test coverage for section grouping, expand/collapse behavior, and FormTypeCard selection.
Output: test/features/inspection/new_inspection_page_test.dart
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-dashboard-new-inspection-redesign/07-CONTEXT.md
@.planning/phases/07-dashboard-new-inspection-redesign/07-02-SUMMARY.md

Relevant source files:
@test/features/inspection/new_inspection_page_test.dart
@lib/features/inspection/presentation/new_inspection_page.dart
@lib/features/inspection/domain/form_type.dart
@lib/features/inspection/domain/inspection_setup.dart
@lib/common/widgets/form_type_card.dart
@lib/common/widgets/app_text_field.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing tests for ExpansionTile-based layout</name>
  <files>test/features/inspection/new_inspection_page_test.dart</files>
  <action>
Read the existing 4 new inspection tests and update finders for the ExpansionTile-based section layout.

**Existing tests to update:**

1. "shows format validation messages before save" — Update for:
   - Form fields are now inside ExpansionTile sections (all sections start expanded, so fields should be visible)
   - Text finders for error messages should still work (validation messages appear below fields)
   - If fields are not findable, ensure ExpansionTiles are expanded in test setup
   - enterText finders may need updating if AppTextField wraps TextFormField differently
   - Preserve: error message assertions, create() not called assertion

2. "enforces at least one selected form" — Update for:
   - Forms are now FormTypeCard widgets, not CheckboxListTile
   - To deselect all forms: tap each FormTypeCard to toggle off (or find checkboxes within FormTypeCard)
   - The "Select at least one inspection form" error message should still appear
   - Preserve: error message assertion, create() not called assertion

3. "shows exact revision form labels" — Update for:
   - Form labels are now inside FormTypeCard widgets
   - Finders should still find the text (FormType.label values)
   - May also verify descriptions are visible (new assertion opportunity)

4. "successful submit saves and navigates to checklist" — Update for:
   - All form fields are inside sections (ensure all are visible/expanded)
   - Form type selection via FormTypeCard tap instead of CheckboxListTile
   - "Continue" button is now in ReachZoneScaffold's stickyBottom area
   - Preserve: create() called once assertion, route assertion, draft extra parameter assertion

**Key principle:** Preserve ALL behavioral assertions. Only change finders to match new widget tree.

**If ExpansionTiles start expanded:** Fields should be immediately findable — minimal finder changes needed.
**If any test can't find a field:** Ensure the section is expanded by tapping the ExpansionTile header first.

> verification: grep -q "validation\|Validation\|validator" test/features/inspection/new_inspection_page_test.dart
> verification: grep -q "at least one" test/features/inspection/new_inspection_page_test.dart
> verification: grep -q "create\|Create" test/features/inspection/new_inspection_page_test.dart
> verification: grep -q "checklist\|Checklist" test/features/inspection/new_inspection_page_test.dart
  </action>
  <verify>
grep -q "validation\|Validation\|validator" test/features/inspection/new_inspection_page_test.dart
grep -q "at least one" test/features/inspection/new_inspection_page_test.dart
grep -q "create\|Create" test/features/inspection/new_inspection_page_test.dart
grep -q "checklist\|Checklist" test/features/inspection/new_inspection_page_test.dart
  </verify>
  <done>All 4 existing tests updated with correct finders for ExpansionTile-based layout, preserving all behavioral assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Add progressive disclosure interaction tests</name>
  <files>test/features/inspection/new_inspection_page_test.dart</files>
  <action>
Add tests for the expand/collapse behavior of form sections.

**Test: "sections start expanded showing all fields"**
- Pump the NewInspectionPage widget
- Assert: All 3 section titles visible ("Client Information", "Property Information", "Inspection Forms")
- Assert: Fields from each section are visible:
  - Client Name field visible
  - Property Address field visible
  - At least one FormTypeCard visible

**Test: "sections can be collapsed and expanded"**
- Pump the widget
- Find the "Client Information" ExpansionTile header
- Tap it to collapse
- pumpAndSettle
- Assert: Client Name field is no longer visible (`findsNothing`)
- Tap the header again to expand
- pumpAndSettle
- Assert: Client Name field is visible again (`findsOneWidget`)

**Test: "collapsed section does not affect other sections"**
- Collapse "Client Information"
- Assert: "Property Information" fields still visible
- Assert: "Inspection Forms" cards still visible

> verification: grep -q "Client Information" test/features/inspection/new_inspection_page_test.dart
> verification: grep -q "Property Information" test/features/inspection/new_inspection_page_test.dart
> verification: grep -q "collapse\|expand\|ExpansionTile" test/features/inspection/new_inspection_page_test.dart
  </action>
  <verify>
grep -q "Client Information" test/features/inspection/new_inspection_page_test.dart
grep -q "Property Information" test/features/inspection/new_inspection_page_test.dart
grep -q "collapse\|expand\|ExpansionTile" test/features/inspection/new_inspection_page_test.dart
  </verify>
  <done>Progressive disclosure tests cover section expand/collapse behavior and independence.</done>
</task>

<task type="auto">
  <name>Task 3: Add FormTypeCard selection and description tests</name>
  <files>test/features/inspection/new_inspection_page_test.dart</files>
  <action>
Add tests for FormTypeCard visual selection and description display.

**Test: "form type cards show descriptions"**
- Pump the widget
- Assert: Find each form type description text:
  - "Electrical, HVAC, plumbing, and water heater inspection" (fourPoint)
  - "Roof age, condition, and remaining useful life assessment" (roofCondition)
  - "Wind resistance features and discount qualification" (windMitigation)
- Assert: `find.byType(FormTypeCard)` finds 3 widgets (one per FormType)

**Test: "form type card toggles selection on tap"**
- Pump the widget (all forms selected by default)
- Find the first FormTypeCard
- Tap it to deselect
- pumpAndSettle
- Assert: The card's selection state changed (visual indicator or checkbox unchecked)
- Tap it again to re-select
- pumpAndSettle
- Assert: Card is selected again
- Note: Selection state can be verified via finding a Checkbox widget within FormTypeCard and checking its value, or by using `tester.widget<FormTypeCard>(finder).selected`

**Test: "can submit with subset of forms selected"**
- Deselect one form type (tap one FormTypeCard)
- Fill in all required fields
- Tap Continue
- Assert: create() called with InspectionSetup that has only 2 forms in enabledForms
- Assert: Navigation to checklist route

**Run all tests:**
`flutter test test/features/inspection/new_inspection_page_test.dart`

All tests must pass. If any fail, investigate and fix the test (not production code — the page was verified in Plan 07-02).

> verification: grep -q "FormTypeCard" test/features/inspection/new_inspection_page_test.dart
> verification: grep -q "Electrical\|HVAC\|plumbing" test/features/inspection/new_inspection_page_test.dart
> verification: grep -q "toggle\|select\|deselect\|selection" test/features/inspection/new_inspection_page_test.dart
> verification: flutter test test/features/inspection/new_inspection_page_test.dart 2>&1 | tail -1 | grep -q "All tests passed"
  </action>
  <verify>
grep -q "FormTypeCard" test/features/inspection/new_inspection_page_test.dart
grep -q "Electrical\|HVAC\|plumbing" test/features/inspection/new_inspection_page_test.dart
grep -q "toggle\|select\|deselect\|selection" test/features/inspection/new_inspection_page_test.dart
flutter test test/features/inspection/new_inspection_page_test.dart 2>&1 | tail -1 | grep -q "All tests passed"
  </verify>
  <done>FormTypeCard tests cover descriptions, selection toggle, and subset submission — all tests pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 4 original tests updated and passing
- [ ] Section expand/collapse tests verify progressive disclosure
- [ ] FormTypeCard description tests verify visual content
- [ ] FormTypeCard selection toggle tests verify interaction
- [ ] Subset form submission test verifies partial selection works
- [ ] `flutter test test/features/inspection/new_inspection_page_test.dart` — all tests pass
</verification>

<success_criteria>
- All original new inspection test behavioral assertions preserved
- New tests cover section expand/collapse, FormTypeCard descriptions, selection toggle
- Subset form submission works correctly
- Test file runs with zero failures
- Test finders accurately match the redesigned widget tree
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-new-inspection-redesign/07-04-SUMMARY.md`

Update STATE.md: Plan 07-04 complete
</output>
