---
phase: 07-dashboard-new-inspection-redesign
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - test/features/inspection/dashboard_page_test.dart
autonomous: false
agents: ["testing-evidence-collector"]
requirements: ["SCREEN-02"]
user_setup: []

must_haves:
  truths:
    - "All 3 existing dashboard tests updated to work with redesigned widget tree"
    - "Empty state test verifies EmptyState widget appears when no inspections"
    - "Metrics summary test verifies correct inspection counts display"
    - "Status badge test verifies correct status per inspection state"
    - "All tests pass: flutter test test/features/inspection/dashboard_page_test.dart"
  artifacts:
    - path: "test/features/inspection/dashboard_page_test.dart"
      provides: "Complete widget test coverage for redesigned dashboard"
      min_lines: 150
      contains: "EmptyState"
  key_links:
    - from: "test/features/inspection/dashboard_page_test.dart"
      to: "lib/features/inspection/presentation/dashboard_page.dart"
      via: "Widget under test"
      pattern: "DashboardPage"
---

<objective>
Update and expand dashboard widget tests to cover the redesigned UI with status indicators, metrics summary, and empty state.

Purpose: Ensure the redesigned dashboard maintains all existing behavioral contracts while gaining test coverage for new features (status badges, metrics, empty state, pull-to-refresh).
Output: test/features/inspection/dashboard_page_test.dart
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-dashboard-new-inspection-redesign/07-CONTEXT.md
@.planning/phases/07-dashboard-new-inspection-redesign/07-01-SUMMARY.md

Relevant source files:
@test/features/inspection/dashboard_page_test.dart
@lib/features/inspection/presentation/dashboard_page.dart
@lib/features/inspection/domain/inspection_wizard_state.dart
@lib/features/inspection/domain/inspection_setup.dart
@lib/common/widgets/status_badge.dart
@lib/common/widgets/empty_state.dart
@lib/common/widgets/reach_zone_scaffold.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing dashboard tests for new widget tree</name>
  <files>test/features/inspection/dashboard_page_test.dart</files>
  <action>
Read the existing 3 dashboard tests and update their widget finders to work with the redesigned UI.

**Existing tests to update:**
1. "dashboard lists in-progress inspections with resume action" — Update finders for:
   - Client name now inside SectionCard/InspectionCard structure
   - "Resume at step 3" text may be in a different widget subtree
   - Resume button may now be an AppButton instead of FilledButton
   - NavigationService.push assertion should remain identical
   - Sync scheduler assertion should remain identical

2. "new inspection button navigates via NavigationService" — Update for:
   - "New Inspection" button is now in ReachZoneScaffold's stickyBottom area
   - May need to scroll or find within the scaffold structure
   - Navigation assertion should remain identical

3. "inspector identity button navigates via NavigationService" — Update for:
   - "Inspector Identity" may now be an AppBar action (IconButton) instead of body button
   - Finder may need to change from text-based to icon-based or use `find.byTooltip`
   - Navigation assertion should remain identical

**Key principle:** Preserve ALL behavioral assertions (verify calls, navigation routes, sync invocations). Only change widget finders to match the new tree structure.

**Test setup:** Preserve existing mock setup (MockNavigationService, SpyInspectionStore, TestSyncScheduler, setupTestServiceLocator/resetServiceLocator pattern).

> verification: grep -q "resume" test/features/inspection/dashboard_page_test.dart
> verification: grep -q "NavigationService" test/features/inspection/dashboard_page_test.dart
> verification: grep -q "syncScheduler\|SyncScheduler\|runPending" test/features/inspection/dashboard_page_test.dart
  </action>
  <verify>
grep -q "resume" test/features/inspection/dashboard_page_test.dart
grep -q "NavigationService" test/features/inspection/dashboard_page_test.dart
grep -q "syncScheduler\|SyncScheduler\|runPending" test/features/inspection/dashboard_page_test.dart
  </verify>
  <done>All 3 existing dashboard tests updated with correct finders for the redesigned widget tree while preserving behavioral assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Add empty state and metrics summary tests</name>
  <files>test/features/inspection/dashboard_page_test.dart</files>
  <action>
Add new tests for the empty state and metrics summary features.

**Test: "shows empty state when no inspections exist"**
- Setup: Configure the mock repository/store to return an empty list
- Pump the DashboardPage widget
- Assert: `find.byType(EmptyState)` finds one widget
- Assert: `find.text('No inspections yet')` or similar empty state message
- Assert: EmptyState has an action button (find text "New Inspection" within EmptyState subtree)
- Optional: Tap the action button, verify navigation to AppRoutes.newInspection

**Test: "shows metrics summary with correct counts"**
- Setup: Configure the mock repository/store to return a list of 3 inspections:
  - 1 in-progress (WizardProgressStatus.inProgress)
  - 1 complete (WizardProgressStatus.complete)
  - 1 draft/fresh (no wizard progress or minimal progress)
- Pump the DashboardPage widget
- Assert: Find text "3" (total count) somewhere in the metrics area
- Assert: Find the individual status counts displayed
- Note: Exact finders depend on how metrics are rendered — use `find.textContaining` if needed

**Test: "hides metrics when inspection list is empty"** (if metrics are hidden on empty)
- Setup: Empty list
- Assert: Metrics row not present OR shows zeros

> verification: grep -q "empty state\|EmptyState\|No inspections" test/features/inspection/dashboard_page_test.dart
> verification: grep -q "metrics\|count" test/features/inspection/dashboard_page_test.dart
  </action>
  <verify>
grep -q "empty state\|EmptyState\|No inspections" test/features/inspection/dashboard_page_test.dart
grep -q "metrics\|count" test/features/inspection/dashboard_page_test.dart
  </verify>
  <done>Empty state and metrics summary tests added, covering zero-data and multi-inspection scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: Add status badge and pull-to-refresh tests</name>
  <files>test/features/inspection/dashboard_page_test.dart</files>
  <action>
Add tests for status badge variants and pull-to-refresh behavior.

**Test: "displays correct status badge for in-progress inspection"**
- Setup: Return 1 in-progress inspection
- Assert: `find.byType(StatusBadge)` finds at least 1
- Assert: Find text "In Progress" (the badge label)

**Test: "displays correct status badge for complete inspection"**
- Setup: Return 1 complete inspection (WizardProgressStatus.complete)
- Assert: Find text "Complete" badge label

**Test: "displays draft status badge for fresh inspection"** (if applicable)
- Setup: Return 1 inspection with no/minimal wizard progress
- Assert: Find text "Draft" badge label

**Test: "pull to refresh reloads inspections"**
- Setup: Return initial list, then configure spy to return updated list on second call
- Perform pull-to-refresh gesture: `await tester.fling(find.byType(RefreshIndicator), const Offset(0, 300), 1000)` or `await tester.drag(...)` + pumpAndSettle
- Assert: Repository's list method called twice (initial + refresh)
- Assert: Updated data displayed after refresh

**Run all tests:**
After adding all tests, run the full test file:
`flutter test test/features/inspection/dashboard_page_test.dart`

All tests must pass. If any test fails, investigate and fix the test (not the production code — the dashboard was verified in Plan 07-01).

> verification: grep -q "StatusBadge" test/features/inspection/dashboard_page_test.dart
> verification: grep -q "In Progress\|Complete\|Draft" test/features/inspection/dashboard_page_test.dart
> verification: grep -q "RefreshIndicator\|refresh\|pull" test/features/inspection/dashboard_page_test.dart
> verification: flutter test test/features/inspection/dashboard_page_test.dart 2>&1 | tail -1 | grep -q "All tests passed"
  </action>
  <verify>
grep -q "StatusBadge" test/features/inspection/dashboard_page_test.dart
grep -q "In Progress\|Complete\|Draft" test/features/inspection/dashboard_page_test.dart
grep -q "RefreshIndicator\|refresh\|pull" test/features/inspection/dashboard_page_test.dart
flutter test test/features/inspection/dashboard_page_test.dart 2>&1 | tail -1 | grep -q "All tests passed"
  </verify>
  <done>Status badge variant tests and pull-to-refresh test added, all dashboard tests pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 3 original tests updated and passing
- [ ] Empty state test covers zero-data scenario
- [ ] Metrics summary test verifies correct counts
- [ ] Status badge tests cover in-progress, complete, and draft variants
- [ ] Pull-to-refresh test verifies data reload
- [ ] `flutter test test/features/inspection/dashboard_page_test.dart` — all tests pass
</verification>

<success_criteria>
- All original dashboard test behavioral assertions preserved
- New tests cover empty state, metrics, status badges, and pull-to-refresh
- Test file runs with zero failures
- Test finders accurately match the redesigned widget tree
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-new-inspection-redesign/07-03-SUMMARY.md`

Update STATE.md: Plan 07-03 complete
</output>
