---
phase: 04-checklist-decomposition
plan: 05
type: execute
wave: 3
depends_on: ["04-04"]
files_modified: []
autonomous: false
agents: ["testing-reality-checker"]
requirements: ["UX-01"]
user_setup: []

must_haves:
  truths:
    - "No sub-view file exceeds 250 lines (verified by wc -l)"
    - "FormChecklistPage is under 350 lines (verified by wc -l)"
    - "InspectionSessionController has zero Flutter imports (verified by grep)"
    - "All ValueKeys from the original monolith exist in the decomposed sub-views"
    - "flutter test passes with zero failures"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 4 constraints are met: line count limits, no functionality loss, test coverage maintained, controller purity, and ValueKey preservation. Fix any violations found.

Purpose: Final quality gate before Phase 4 is declared complete. Reality-checker agent ensures no requirements were silently dropped during decomposition.
Output: Verification report with pass/fail per constraint.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-checklist-decomposition/04-CONTEXT.md
@.planning/phases/04-checklist-decomposition/04-01-SUMMARY.md
@.planning/phases/04-checklist-decomposition/04-02-SUMMARY.md
@.planning/phases/04-checklist-decomposition/04-03-SUMMARY.md
@.planning/phases/04-checklist-decomposition/04-04-SUMMARY.md
@.planning/specs/04-checklist-decomposition-spec.md

Relevant source files:
@lib/features/inspection/presentation/form_checklist_page.dart
@lib/features/inspection/presentation/controllers/inspection_session_controller.dart
@lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart
@lib/features/inspection/presentation/sub_views/evidence_capture_view.dart
@lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart
@lib/features/inspection/presentation/sub_views/audit_timeline_view.dart
@lib/features/inspection/presentation/shared_widgets/branch_flag_toggle_tile.dart
@lib/features/inspection/presentation/shared_widgets/evidence_requirement_card.dart
@test/features/inspection/form_checklist_page_test.dart
@test/features/inspection/controllers/inspection_session_controller_test.dart
@test/features/inspection/sub_views/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify line count and structural constraints</name>
  <files></files>
  <action>
Run the following checks and produce a report:

**Line count checks:**
```bash
echo "=== Line Count Report ==="
echo "FormChecklistPage:" && wc -l < lib/features/inspection/presentation/form_checklist_page.dart
echo "InspectionSessionController:" && wc -l < lib/features/inspection/presentation/controllers/inspection_session_controller.dart
echo "WizardNavigationView:" && wc -l < lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart
echo "EvidenceCaptureView:" && wc -l < lib/features/inspection/presentation/sub_views/evidence_capture_view.dart
echo "PdfDeliveryView:" && wc -l < lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart
echo "AuditTimelineView:" && wc -l < lib/features/inspection/presentation/sub_views/audit_timeline_view.dart
echo "BranchFlagToggleTile:" && wc -l < lib/features/inspection/presentation/shared_widgets/branch_flag_toggle_tile.dart
echo "EvidenceRequirementCard:" && wc -l < lib/features/inspection/presentation/shared_widgets/evidence_requirement_card.dart
```

**Constraints to verify:**
| Constraint | Target | Check Command |
|-----------|--------|---------------|
| Sub-views <= 250 lines each | wizard, evidence, pdf, audit | `wc -l` on each |
| Parent <= 350 lines | form_checklist_page.dart | `wc -l` |
| Controller is pure Dart | No Flutter imports | `grep "package:flutter" controller.dart` should return nothing |
| 4 sub-views exist | Files present | `ls lib/.../sub_views/` |
| 2 shared widgets exist | Files present | `ls lib/.../shared_widgets/` |
| Controller exists | File present | `test -f lib/.../controllers/inspection_session_controller.dart` |

**ValueKey preservation:**
Extract all ValueKeys from the original monolith (from git history or the spec) and verify they exist in the new files:
- `'generate-pdf-button'` → PdfDeliveryView
- `'delivery-download-button'` → PdfDeliveryView
- `'delivery-secure-share-button'` → PdfDeliveryView
- `'branch-flag-*'` → BranchFlagToggleTile

```bash
echo "=== ValueKey Checks ==="
grep -r "generate-pdf-button" lib/features/inspection/presentation/
grep -r "delivery-download-button" lib/features/inspection/presentation/
grep -r "delivery-secure-share-button" lib/features/inspection/presentation/
grep -r "branch-flag-" lib/features/inspection/presentation/
```

**If any constraint is violated:** Fix the violation directly. If a sub-view exceeds 250 lines, extract more into shared widgets or simplify. If the parent exceeds 350 lines, move more logic to the controller.

> verification: wc -l < lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart | xargs test 250 -ge
> verification: wc -l < lib/features/inspection/presentation/sub_views/evidence_capture_view.dart | xargs test 250 -ge
> verification: wc -l < lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart | xargs test 250 -ge
> verification: wc -l < lib/features/inspection/presentation/sub_views/audit_timeline_view.dart | xargs test 250 -ge
> verification: wc -l < lib/features/inspection/presentation/form_checklist_page.dart | xargs test 350 -ge
> verification: ! grep -q "package:flutter" lib/features/inspection/presentation/controllers/inspection_session_controller.dart
  </action>
  <verify>
wc -l < lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart | xargs test 250 -ge
wc -l < lib/features/inspection/presentation/sub_views/evidence_capture_view.dart | xargs test 250 -ge
wc -l < lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart | xargs test 250 -ge
wc -l < lib/features/inspection/presentation/sub_views/audit_timeline_view.dart | xargs test 250 -ge
wc -l < lib/features/inspection/presentation/form_checklist_page.dart | xargs test 350 -ge
! grep -q "package:flutter" lib/features/inspection/presentation/controllers/inspection_session_controller.dart
  </verify>
  <done>All line count and structural constraints verified — sub-views under 250 LOC, parent under 350 LOC, controller is pure Dart, all ValueKeys preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Verify functionality preservation and test coverage</name>
  <files></files>
  <action>
**Functionality checklist** — verify each feature from the original monolith exists in the decomposed structure:

| Feature | Original Location | Expected New Location | Check |
|---------|------------------|----------------------|-------|
| Wizard step navigation | _continueStep() | Controller.continueStep() | grep controller |
| Branch flag toggles | _setBranchFlag(), _buildBranchInputControls() | Controller.setBranchFlag(), WizardNavigationView + BranchFlagToggleTile | grep sub_views + shared_widgets |
| Evidence capture | _capture() | Controller.capture() | grep controller |
| PDF generation (cloud fallback) | _generatePdf() | Controller.generatePdf() | grep controller |
| Delivery download | _downloadLastArtifact() | Controller.downloadArtifact() | grep controller |
| Delivery share | _shareLastArtifact() | Controller.shareArtifact() | grep controller |
| Audit timeline | _loadAuditEvents(), _buildAuditTimelineSection() | Controller.loadAuditEvents(), AuditTimelineView | grep both |
| Readiness evaluation | _loadReadiness(), _evaluateReadiness() | Controller | grep controller |
| Evidence path merging | _loadEvidenceMediaPaths(), _mergeEvidencePaths() | Controller | grep controller |
| Progress saving | _saveProgress() | Controller | grep controller |
| Branch context persistence | _setBranchFlag() | Controller.setBranchFlag() | grep controller |

```bash
echo "=== Functionality Checks ==="
grep -q "continueStep" lib/features/inspection/presentation/controllers/inspection_session_controller.dart && echo "PASS: continueStep" || echo "FAIL: continueStep"
grep -q "capture" lib/features/inspection/presentation/controllers/inspection_session_controller.dart && echo "PASS: capture" || echo "FAIL: capture"
grep -q "generatePdf" lib/features/inspection/presentation/controllers/inspection_session_controller.dart && echo "PASS: generatePdf" || echo "FAIL: generatePdf"
grep -q "downloadArtifact\|downloadLastArtifact" lib/features/inspection/presentation/controllers/inspection_session_controller.dart && echo "PASS: download" || echo "FAIL: download"
grep -q "shareArtifact\|shareLastArtifact" lib/features/inspection/presentation/controllers/inspection_session_controller.dart && echo "PASS: share" || echo "FAIL: share"
grep -q "loadAuditEvents" lib/features/inspection/presentation/controllers/inspection_session_controller.dart && echo "PASS: auditEvents" || echo "FAIL: auditEvents"
grep -q "setBranchFlag" lib/features/inspection/presentation/controllers/inspection_session_controller.dart && echo "PASS: branchFlag" || echo "FAIL: branchFlag"
grep -q "loadReadiness\|_loadReadiness" lib/features/inspection/presentation/controllers/inspection_session_controller.dart && echo "PASS: readiness" || echo "FAIL: readiness"
```

**Test coverage check:**
```bash
flutter test 2>&1 | tail -10
```

Verify:
- All tests pass (zero failures)
- Test count >= 18 (original) + 10 (controller) + 16 (sub-views) = 44+ total test cases across all files
- Count testWidgets in each file:
```bash
echo "=== Test Count ==="
grep -c "testWidgets\|test(" test/features/inspection/form_checklist_page_test.dart
grep -c "testWidgets\|test(" test/features/inspection/controllers/inspection_session_controller_test.dart
grep -c "testWidgets\|test(" test/features/inspection/sub_views/*.dart 2>/dev/null
```

**If any functionality is missing:** Add it back to the controller or appropriate sub-view and add a test.

> verification: flutter test 2>&1 | grep -E "All tests passed|tests passed"
  </action>
  <verify>
flutter test 2>&1 | grep -E "All tests passed|tests passed"
  </verify>
  <done>All functionality preserved — every feature from the original monolith exists in the decomposed structure, all tests pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All sub-views <= 250 lines
- [ ] FormChecklistPage <= 350 lines
- [ ] InspectionSessionController has zero Flutter imports
- [ ] All ValueKeys preserved
- [ ] All 11 features from monolith exist in decomposed structure
- [ ] `flutter test` passes with zero failures
- [ ] Test count >= 44
</verification>

<success_criteria>
- Zero constraint violations
- Zero functionality gaps
- All tests pass
- Verification report produced
</success_criteria>

<output>
After completion, create `.planning/phases/04-checklist-decomposition/04-05-SUMMARY.md`

Update STATE.md:
- Phase: 4 of 8 (complete)
- Status: Phase 4 complete — awaiting review
- Next Action: Run `/legion:review` to review Phase 4
</output>
