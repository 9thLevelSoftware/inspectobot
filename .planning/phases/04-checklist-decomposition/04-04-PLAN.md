---
phase: 04-checklist-decomposition
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - test/features/inspection/form_checklist_page_test.dart
  - test/features/inspection/helpers/checklist_test_helpers.dart
  - test/features/inspection/sub_views/wizard_navigation_view_test.dart
  - test/features/inspection/sub_views/evidence_capture_view_test.dart
  - test/features/inspection/sub_views/pdf_delivery_view_test.dart
  - test/features/inspection/sub_views/audit_timeline_view_test.dart
autonomous: false
agents: ["engineering-mobile-app-builder"]
requirements: ["UX-01"]
user_setup: []

must_haves:
  truths:
    - "All 18 original test behaviors are preserved (same assertions, updated widget finders)"
    - "Mock/stub classes are extracted to shared test helpers to avoid duplication across 6 test files"
    - "Integration tests verify tab switching and cross-view state synchronization"
    - "All tests pass with flutter test"
    - "No test file exceeds 500 lines"
  artifacts:
    - path: "test/features/inspection/helpers/checklist_test_helpers.dart"
      provides: "Shared mock classes and test fixture builders for checklist tests"
      min_lines: 100
      contains: "class"
    - path: "test/features/inspection/form_checklist_page_test.dart"
      provides: "Refactored integration tests for the decomposed checklist page"
      min_lines: 200
      contains: "testWidgets"
  key_links:
    - from: "test/features/inspection/form_checklist_page_test.dart"
      to: "test/features/inspection/helpers/checklist_test_helpers.dart"
      via: "Shared mock imports"
      pattern: "checklist_test_helpers"
---

<objective>
Refactor the 1,453-line test file to work with the decomposed page structure, extract shared test helpers, and add integration tests for tab switching and cross-view state synchronization.

Purpose: Ensure test coverage is maintained and improved after decomposition. All 18 original test behaviors must be preserved.
Output: Refactored integration tests, shared helpers, updated sub-view tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-checklist-decomposition/04-CONTEXT.md
@.planning/phases/04-checklist-decomposition/04-01-SUMMARY.md
@.planning/phases/04-checklist-decomposition/04-02-SUMMARY.md
@.planning/phases/04-checklist-decomposition/04-03-SUMMARY.md
@.planning/specs/04-checklist-decomposition-spec.md

Relevant source files:
@test/features/inspection/form_checklist_page_test.dart
@lib/features/inspection/presentation/form_checklist_page.dart
@lib/features/inspection/presentation/controllers/inspection_session_controller.dart
@lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart
@lib/features/inspection/presentation/sub_views/evidence_capture_view.dart
@lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart
@lib/features/inspection/presentation/sub_views/audit_timeline_view.dart
@test/features/inspection/sub_views/
@test/features/inspection/controllers/inspection_session_controller_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared test helpers</name>
  <files>test/features/inspection/helpers/checklist_test_helpers.dart</files>
  <action>
Create `test/features/inspection/helpers/checklist_test_helpers.dart`.

Extract all mock/stub/fake classes from the existing `form_checklist_page_test.dart` (1,453 lines) into this shared helper file. This prevents duplication across the 6 test files.

**Extract these classes** (reference the current test file):
- `_ChecklistStore` (fake InspectionRepository with updateCalls tracking)
- `_StaticCloudPdfRuntimeGateway`
- `_FixedOutcomeCloudPdfService`
- `_SuccessfulOnDevicePdfService`
- `_OverBudgetOnDevicePdfService`
- `_CapturingOnDevicePdfService`
- `_FakePendingMediaSyncStore`
- `_FailingSignatureEvidenceRepository`
- `_RecordingAuditEventGateway` / `_FailingAuditEventGateway`
- `_TestSignedUrlGateway` / `_TestShareGateway`

**Rename from private to public** (remove leading underscore since they're now in a shared file):
- `_ChecklistStore` → `FakeChecklistStore`
- `_FakePendingMediaSyncStore` → `FakePendingMediaSyncStore`
- etc.

**Add test fixture builders:**
- `InspectionDraft buildTestDraft({...})` — creates a test InspectionDraft with sensible defaults
- `WizardProgressSnapshot buildTestSnapshot({...})` — creates a test snapshot with optional overrides
- `FormChecklistPage buildTestPage({...})` — creates the page with mock dependencies wired up, for integration tests

**Add a pumpChecklistPage helper:**
```dart
Future<void> pumpChecklistPage(
  WidgetTester tester, {
  InspectionDraft? draft,
  FakeChecklistStore? store,
  // ... other overrides
}) async {
  await tester.pumpWidget(
    MaterialApp(
      theme: AppTheme.dark(),
      home: FormChecklistPage(
        draft: draft ?? buildTestDraft(),
        repository: store ?? FakeChecklistStore(),
        // ... wire up defaults
      ),
    ),
  );
}
```

> verification: test -f test/features/inspection/helpers/checklist_test_helpers.dart
> verification: grep -q "FakeChecklistStore" test/features/inspection/helpers/checklist_test_helpers.dart
> verification: grep -q "buildTestDraft" test/features/inspection/helpers/checklist_test_helpers.dart
  </action>
  <verify>
test -f test/features/inspection/helpers/checklist_test_helpers.dart
grep -q "FakeChecklistStore" test/features/inspection/helpers/checklist_test_helpers.dart
grep -q "buildTestDraft" test/features/inspection/helpers/checklist_test_helpers.dart
  </verify>
  <done>Shared test helpers extracted with public mock classes and fixture builders.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor integration tests</name>
  <files>test/features/inspection/form_checklist_page_test.dart</files>
  <action>
Refactor `test/features/inspection/form_checklist_page_test.dart` to work with the decomposed page.

**Key changes needed:**
1. Import shared helpers instead of defining mock classes inline
2. Update widget finders that now need to find widgets inside sub-views (they should still work since sub-views are composed in the widget tree, but verify)
3. Add tab navigation steps where tests need to access widgets on non-default tabs:
   - Tests that check PDF generation button → need to switch to "Report" tab first
   - Tests that check audit timeline → need to switch to "Timeline" tab first
   - Tests that check wizard steps → should work on default "Steps" tab
4. Preserve all 18 test behaviors — same assertions, same expected outcomes

**Tab switching helper:**
```dart
Future<void> switchToTab(WidgetTester tester, int tabIndex) async {
  // Find SegmentedButton and tap the appropriate segment
  // Or find the tab by label text
  await tester.tap(find.text('Report')); // for PDF tab
  await tester.pumpAndSettle();
}
```

**Tests to update (from the smoke report in 04-03-SUMMARY):**
- Reference the specific failures documented in Plan 04-03's test smoke report
- Fix each failing test by updating finders and adding tab navigation steps
- Do NOT delete any test — every original behavior must be covered

**Add new integration tests for tab navigation:**
- Test: switching between all 4 tabs renders correct sub-views
- Test: state changes on one tab reflect on another (e.g., capture evidence on Steps tab → Summary tab shows updated completion)
- Test: tab selection persists across state updates (tab doesn't reset after setState)

**Target:** Keep this file under 500 lines by using shared helpers.

> verification: grep -c "testWidgets" test/features/inspection/form_checklist_page_test.dart | xargs test 18 -le
> verification: wc -l < test/features/inspection/form_checklist_page_test.dart | xargs test 500 -ge
  </action>
  <verify>
grep -c "testWidgets" test/features/inspection/form_checklist_page_test.dart | xargs test 18 -le
wc -l < test/features/inspection/form_checklist_page_test.dart | xargs test 500 -ge
  </verify>
  <done>Integration tests refactored — all 18 original behaviors preserved, tab navigation tests added, shared helpers imported.</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and fix failures</name>
  <files></files>
  <action>
Run the complete test suite and fix any remaining failures:

```bash
flutter test
```

**Process:**
1. Run all tests
2. If any fail:
   a. Categorize failures: import errors, finder mismatches, state timing issues, mock API changes
   b. Fix each failure at the source (update the appropriate test file)
   c. Re-run until all pass
3. Count total tests passing vs. baseline (should be >= original count + new sub-view tests + new controller tests)

**Expected test counts:**
- Controller tests: ~15 (new)
- Sub-view tests: ~20 across 4 files (new)
- Integration tests: 18+ (original preserved + tab nav additions)
- Other existing tests: unchanged

**If a test is genuinely impossible to preserve** (e.g., it tested an internal implementation detail that no longer exists), document why and ensure the behavior is covered by an equivalent test in the new structure.

> verification: flutter test 2>&1 | grep -E "All tests passed|tests passed"
  </action>
  <verify>
flutter test 2>&1 | grep -E "All tests passed|tests passed"
  </verify>
  <done>Full test suite passes — all original behaviors preserved, new tests for controller and sub-views added.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 18 original test behaviors are preserved
- [ ] Shared test helpers file exists with extracted mocks
- [ ] Integration tests include tab navigation and cross-view state sync
- [ ] `flutter test` passes with all tests
- [ ] No test file exceeds 500 lines
</verification>

<success_criteria>
- `flutter test` passes with zero failures
- Test count is >= original count + new sub-view/controller tests
- All 18 original test behaviors verified (not deleted or weakened)
- Shared test helpers eliminate mock duplication
</success_criteria>

<output>
After completion, create `.planning/phases/04-checklist-decomposition/04-04-SUMMARY.md`
</output>
