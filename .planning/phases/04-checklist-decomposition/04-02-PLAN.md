---
phase: 04-checklist-decomposition
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart
  - lib/features/inspection/presentation/sub_views/evidence_capture_view.dart
  - lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart
  - lib/features/inspection/presentation/sub_views/audit_timeline_view.dart
  - test/features/inspection/sub_views/wizard_navigation_view_test.dart
  - test/features/inspection/sub_views/evidence_capture_view_test.dart
  - test/features/inspection/sub_views/pdf_delivery_view_test.dart
  - test/features/inspection/sub_views/audit_timeline_view_test.dart
autonomous: false
agents: ["engineering-mobile-app-builder"]
requirements: ["UX-01"]
user_setup: []

must_haves:
  truths:
    - "Each sub-view is a StatelessWidget (no business logic, no service dependencies)"
    - "Each sub-view receives read-only data + callbacks as constructor parameters"
    - "No sub-view file exceeds 250 lines"
    - "Sub-views use design tokens from lib/theme/ and components from lib/common/widgets/ — zero hardcoded colors/spacing"
    - "Each sub-view has a dedicated widget test file that renders it in isolation with mocked data"
  artifacts:
    - path: "lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart"
      provides: "Wizard step display with branch controls and evidence capture"
      min_lines: 80
      contains: "class WizardNavigationView"
    - path: "lib/features/inspection/presentation/sub_views/evidence_capture_view.dart"
      provides: "Per-form completion summary cards"
      min_lines: 50
      contains: "class EvidenceCaptureView"
    - path: "lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart"
      provides: "PDF generation and delivery action buttons"
      min_lines: 60
      contains: "class PdfDeliveryView"
    - path: "lib/features/inspection/presentation/sub_views/audit_timeline_view.dart"
      provides: "Audit event timeline with loading/error/empty states"
      min_lines: 50
      contains: "class AuditTimelineView"
  key_links:
    - from: "lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart"
      to: "lib/features/inspection/presentation/shared_widgets/branch_flag_toggle_tile.dart"
      via: "Widget composition"
      pattern: "BranchFlagToggleTile"
    - from: "lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart"
      to: "lib/features/inspection/presentation/shared_widgets/evidence_requirement_card.dart"
      via: "Widget composition"
      pattern: "EvidenceRequirementCard"
---

<objective>
Build four focused sub-views (WizardNavigationView, EvidenceCaptureView, PdfDeliveryView, AuditTimelineView) as StatelessWidgets that render specific sections of the checklist page, receiving data and callbacks from the parent.

Purpose: Create the UI layer that replaces inline build methods in the monolith. Each sub-view is testable in isolation and respects the 250-line limit.
Output: 4 sub-view files + 4 sub-view test files.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-checklist-decomposition/04-CONTEXT.md
@.planning/phases/04-checklist-decomposition/04-01-SUMMARY.md
@.planning/specs/04-checklist-decomposition-spec.md

Relevant source files:
@lib/features/inspection/presentation/form_checklist_page.dart
@lib/features/inspection/presentation/controllers/inspection_session_controller.dart
@lib/features/inspection/presentation/shared_widgets/branch_flag_toggle_tile.dart
@lib/features/inspection/presentation/shared_widgets/evidence_requirement_card.dart
@lib/features/inspection/domain/inspection_wizard_state.dart
@lib/features/inspection/domain/evidence_requirement.dart
@lib/features/inspection/domain/report_readiness.dart
@lib/features/delivery/domain/report_artifact.dart
@lib/features/audit/domain/audit_event.dart
@lib/common/widgets/
@lib/theme/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WizardNavigationView and EvidenceCaptureView</name>
  <files>lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart, lib/features/inspection/presentation/sub_views/evidence_capture_view.dart</files>
  <action>
**WizardNavigationView** (`lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart`):

Extract the monolith's `_buildStepContent()` method (lines 607-648), branch controls (lines 586-605), and the step header + continue button (lines 736-751) into a StatelessWidget.

Props:
- `InspectionWizardState wizardState` — for step list and completion checks
- `int currentStepIndex` — current step
- `WizardProgressSnapshot snapshot` — for completion map and branch context
- `bool isSavingProgress` — loading state for continue button
- `void Function(EvidenceRequirement) onCapture` — capture callback
- `VoidCallback onContinue` — continue/finish callback
- `void Function(String key, bool value) onSetBranchFlag` — branch flag callback

Renders:
1. Step header: "Step {X} of {Y}: {title}" (use SectionHeader from component library)
2. Branch flag toggles for current step's form (use BranchFlagToggleTile)
3. Evidence requirement cards for current step (use EvidenceRequirementCard)
4. Continue/Finish FilledButton (disabled when can't advance or saving)

Reference the monolith's static maps `_branchFlagsByForm` and `_branchFlagLabels` — these now live in the controller. Pass the relevant flags for the current step as a parameter, or import from controller.

Use design tokens for all spacing (e.g., `AppSpacing.md` instead of `EdgeInsets.all(16)`).

Target: 120-150 lines.

**EvidenceCaptureView** (`lib/features/inspection/presentation/sub_views/evidence_capture_view.dart`):

Extract the monolith's per-form summary section (lines 753-771) into a StatelessWidget.

Props:
- `List<FormProgressSummary> formSummaries` — from wizardState.buildFormSummaries()

Renders:
1. Section header: "Per-Form Summary" (use SectionHeader)
2. For each summary: a Card with ListTile showing form label, completion status ("Complete" / "Missing required: ..."), and trailing check/error icon
3. Use StatusBadge from component library for complete/incomplete status

Target: 80-120 lines.

> verification: test -f lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart
> verification: test -f lib/features/inspection/presentation/sub_views/evidence_capture_view.dart
> verification: grep -q "class WizardNavigationView" lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart
> verification: grep -q "class EvidenceCaptureView" lib/features/inspection/presentation/sub_views/evidence_capture_view.dart
> verification: grep -q "StatelessWidget" lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart
> verification: grep -q "BranchFlagToggleTile" lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart
> verification: wc -l < lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart | xargs test 250 -ge
> verification: wc -l < lib/features/inspection/presentation/sub_views/evidence_capture_view.dart | xargs test 250 -ge
  </action>
  <verify>
test -f lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart
test -f lib/features/inspection/presentation/sub_views/evidence_capture_view.dart
grep -q "class WizardNavigationView" lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart
grep -q "class EvidenceCaptureView" lib/features/inspection/presentation/sub_views/evidence_capture_view.dart
wc -l < lib/features/inspection/presentation/sub_views/wizard_navigation_view.dart | xargs test 250 -ge
wc -l < lib/features/inspection/presentation/sub_views/evidence_capture_view.dart | xargs test 250 -ge
  </verify>
  <done>WizardNavigationView and EvidenceCaptureView exist as StatelessWidgets under 250 lines, using shared widgets and design tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Create PdfDeliveryView and AuditTimelineView</name>
  <files>lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart, lib/features/inspection/presentation/sub_views/audit_timeline_view.dart</files>
  <action>
**PdfDeliveryView** (`lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart`):

Extract the monolith's PDF generation button (lines 775-792) and delivery actions (lines 793-817) into a StatelessWidget.

Props:
- `ReportReadiness readiness` — for ready/blocked state
- `bool isComplete` — wizard completion state
- `bool isGenerating` — loading state
- `String? lastPdfPath` — path to display after generation
- `ReportArtifact? lastArtifact` — triggers delivery buttons visibility
- `VoidCallback onGeneratePdf` — generate callback
- `VoidCallback onDownload` — download callback
- `VoidCallback onShare` — share callback

Renders:
1. Generate PDF FilledButton.icon with:
   - Icon: CircularProgressIndicator (16x16, strokeWidth 2) when generating, Icons.picture_as_pdf otherwise
   - Label: "Generate PDF" when ready, "Readiness blocked: {missing items}" when not
   - Disabled when: not complete OR not ready OR generating
   - Uses ValueKey('generate-pdf-button')
2. Last PDF path (SelectableText) when available
3. Download + Secure Share OutlinedButton.icons when lastArtifact is not null
   - Download: ValueKey('delivery-download-button'), Icons.download
   - Share: ValueKey('delivery-secure-share-button'), Icons.ios_share
   - Wrapped in Wrap(spacing: 8, runSpacing: 8)

Preserve ALL ValueKeys from the monolith — tests depend on them.

Target: 100-120 lines.

**AuditTimelineView** (`lib/features/inspection/presentation/sub_views/audit_timeline_view.dart`):

Extract the monolith's `_buildAuditTimelineSection()` method (lines 658-713) and `_formatAuditTimestamp()` (lines 651-656) into a StatelessWidget.

Props:
- `List<AuditEvent> auditEvents`
- `bool isLoading`
- `String? errorMessage`

Renders exactly the same UI as the current monolith:
1. Loading state: Card with "Loading audit timeline..." title
2. Error state: Card with error icon (redAccent) and error message
3. Empty state: Card with timeline icon and "No audit events recorded yet"
4. Populated state: "Audit Timeline" header + event cards (max 12, Icons.history, timelineLabel, formatted timestamp)

Include `_formatAuditTimestamp` as a static helper method in this file.

Use design tokens for colors (e.g., theme error color instead of hardcoded `Colors.redAccent`). Use component library widgets (EmptyState, ErrorBanner) where they match the UI pattern.

Target: 80-100 lines.

> verification: test -f lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart
> verification: test -f lib/features/inspection/presentation/sub_views/audit_timeline_view.dart
> verification: grep -q "class PdfDeliveryView" lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart
> verification: grep -q "class AuditTimelineView" lib/features/inspection/presentation/sub_views/audit_timeline_view.dart
> verification: grep -q "generate-pdf-button" lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart
> verification: grep -q "delivery-download-button" lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart
> verification: wc -l < lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart | xargs test 250 -ge
> verification: wc -l < lib/features/inspection/presentation/sub_views/audit_timeline_view.dart | xargs test 250 -ge
  </action>
  <verify>
test -f lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart
test -f lib/features/inspection/presentation/sub_views/audit_timeline_view.dart
grep -q "class PdfDeliveryView" lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart
grep -q "class AuditTimelineView" lib/features/inspection/presentation/sub_views/audit_timeline_view.dart
grep -q "generate-pdf-button" lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart
wc -l < lib/features/inspection/presentation/sub_views/pdf_delivery_view.dart | xargs test 250 -ge
wc -l < lib/features/inspection/presentation/sub_views/audit_timeline_view.dart | xargs test 250 -ge
  </verify>
  <done>PdfDeliveryView and AuditTimelineView exist as StatelessWidgets under 250 lines, preserving all ValueKeys and matching monolith UI output.</done>
</task>

<task type="auto">
  <name>Task 3: Create sub-view widget tests</name>
  <files>test/features/inspection/sub_views/wizard_navigation_view_test.dart, test/features/inspection/sub_views/evidence_capture_view_test.dart, test/features/inspection/sub_views/pdf_delivery_view_test.dart, test/features/inspection/sub_views/audit_timeline_view_test.dart</files>
  <action>
Create 4 widget test files, one per sub-view. Each tests the sub-view in isolation by providing mock data and verifying correct rendering.

**wizard_navigation_view_test.dart** (~200 lines):
- Test step header displays correct "Step X of Y: Title"
- Test branch flag toggles appear for form steps with branch flags
- Test evidence requirement cards render with correct captured/missing state
- Test Continue button is disabled when step is incomplete
- Test Continue button shows "Finish Wizard" on last step
- Test Continue button shows "Saving..." when isSavingProgress is true
- Test onCapture callback fires when capture button tapped
- Test onContinue callback fires when continue button tapped

**evidence_capture_view_test.dart** (~150 lines):
- Test form summary cards render for each FormProgressSummary
- Test complete forms show check icon
- Test incomplete forms show error icon with missing requirement labels
- Test empty summaries list renders correctly

**pdf_delivery_view_test.dart** (~200 lines):
- Test generate button disabled when not ready
- Test generate button enabled when ready and complete
- Test generate button shows loading spinner when isGenerating
- Test readiness blocked message shows missing items
- Test last PDF path displays as SelectableText
- Test download/share buttons appear when lastArtifact is not null
- Test download/share buttons hidden when lastArtifact is null
- Test ValueKeys are present: 'generate-pdf-button', 'delivery-download-button', 'delivery-secure-share-button'

**audit_timeline_view_test.dart** (~150 lines):
- Test loading state shows "Loading audit timeline..."
- Test error state shows error icon and message
- Test empty state shows "No audit events recorded yet"
- Test populated state shows event cards with timeline labels
- Test max 12 events displayed even if more exist
- Test timestamp formatting

Use `tester.pumpWidget(MaterialApp(theme: AppTheme.dark(), home: Scaffold(body: ...)))` pattern for all widget tests. Create minimal mock data (InspectionWizardState, FormProgressSummary, AuditEvent, ReportReadiness) as test fixtures.

> verification: test -f test/features/inspection/sub_views/wizard_navigation_view_test.dart
> verification: test -f test/features/inspection/sub_views/evidence_capture_view_test.dart
> verification: test -f test/features/inspection/sub_views/pdf_delivery_view_test.dart
> verification: test -f test/features/inspection/sub_views/audit_timeline_view_test.dart
> verification: grep -c "testWidgets" test/features/inspection/sub_views/wizard_navigation_view_test.dart | xargs test 4 -le
> verification: grep -c "testWidgets" test/features/inspection/sub_views/pdf_delivery_view_test.dart | xargs test 4 -le
  </action>
  <verify>
test -f test/features/inspection/sub_views/wizard_navigation_view_test.dart
test -f test/features/inspection/sub_views/evidence_capture_view_test.dart
test -f test/features/inspection/sub_views/pdf_delivery_view_test.dart
test -f test/features/inspection/sub_views/audit_timeline_view_test.dart
grep -c "testWidgets" test/features/inspection/sub_views/wizard_navigation_view_test.dart | xargs test 4 -le
grep -c "testWidgets" test/features/inspection/sub_views/pdf_delivery_view_test.dart | xargs test 4 -le
  </verify>
  <done>All 4 sub-view test files exist with widget tests covering rendering states, callbacks, and edge cases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 4 sub-view files exist under lib/features/inspection/presentation/sub_views/
- [ ] Each sub-view is a StatelessWidget
- [ ] No sub-view exceeds 250 lines
- [ ] All sub-views use design tokens (no hardcoded colors/spacing)
- [ ] All ValueKeys from monolith are preserved in sub-views
- [ ] All 4 sub-view test files exist with widget tests
- [ ] Sub-view tests pass: `flutter test test/features/inspection/sub_views/`
- [ ] Existing tests still pass: `flutter test`
</verification>

<success_criteria>
- 4 sub-view files exist, each under 250 lines
- Sub-views are pure widgets (StatelessWidget, no service dependencies)
- Sub-views use shared widgets (BranchFlagToggleTile, EvidenceRequirementCard) and component library
- 4 sub-view test files pass independently
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-checklist-decomposition/04-02-SUMMARY.md`
</output>
